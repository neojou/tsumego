# v1 解題搜尋用迭代加深 α-β／negamax，不問 df-pn

解題器只問二元問題：黑能否強迫題型「過」。v1 用迭代加深的證明搜尋，葉子走分類器；不設時限，已走完的搜尋路徑見 ADR-0019。類別名是 `AlphaBetaSolver`，程式是 AND–OR（黑 OR、白 AND），沒有 α／β 視窗。現況對照見 `docs/decision.md`。df-pn 是文獻上更適合證明的算法，但 v1 不承擔其實作風險。算法在解題模組內部，以後可換，不改 ADR-0012 的介面。
